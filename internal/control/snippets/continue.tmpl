# Continue.dev → Opod (OpenAI API)
# Add to ~/.continue/config.json under "models":

{
  "title": "Opod · {{.Model}}",
  "provider": "openai",
  "model": "{{.Model}}",
  "apiBase": "{{.BaseURL}}/v1",
  "apiKey": "{{.Token}}"
}
