# Hermes Agent → Opod (Anthropic API; same wire format as Claude Code)

export ANTHROPIC_BASE_URL=http://example.local:8080
export ANTHROPIC_AUTH_TOKEN=sk-orc-TEST
export ANTHROPIC_MODEL=test-model

hermes

# Hermes auto-discovers tool definitions per session; persistent memory
# lives in ~/.hermes/. Switch model mid-session with `/model <id>`.
