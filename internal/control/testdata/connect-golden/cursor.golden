# Cursor → Opod (OpenAI API)
# Settings → Models → Override OpenAI Base URL

Base URL:  http://example.local:8080/v1
API Key:   sk-orc-TEST
Model:     test-model

# Then in Cursor: ⌘L (chat) or ⌘K (inline) — picks the model above.
